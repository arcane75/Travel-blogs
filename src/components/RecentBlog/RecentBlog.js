import { Typography } from '@mui/material';
import React from 'react';

const RecentBlog = () => {
    return (
        <div>
            <Typography sx={{textAlign:'center', fontWeight:'bold'}}>Latest Offer</Typography>
        </div>
    );
};

export default RecentBlog;